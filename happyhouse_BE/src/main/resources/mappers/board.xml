<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.ssafy.happyhouse.mapper.BoardMapper">

	<insert id="registerBoard" parameterType="BoardDto">
		insert into board (memberId, boardTitle, boardContent)
		values (#{memberId}, #{boardTitle}, #{boardContent})
	</insert>

	<sql id="search">
		<if test="word != null and word != ''">
			<choose>
				<when test="key.equals('all')">
					boardTitle like concat('%', #{word}, '%') or boardContent like concat('%', #{word}, '%')
				</when>
				<when test="key.equals('boardTitle')">
					boardTitle like concat('%', #{word}, '%')
				</when>
				<when test="key.equals('boardContent')">
					boardContent like concat('%', #{word}, '%')
				</when>
			</choose>
		</if>
	</sql>
	
	<select id="listBoard" parameterType="map" resultType="BoardDto">
		select *
		from board
		order by regtime desc
	</select>

	<select id="getTotalCount" parameterType="map" resultType="integer">
		select count(*)
		from board
		<where>
			<include refid="search"></include>
		</where>		
	</select>
	
	<select id="getBoard" parameterType="integer" resultType="BoardDto">
		select *
		from Board
		where boardid = #{boardId}
	</select>
	
	<update id="updateBoard" parameterType="BoardDto">
		update Board
		set boardtitle = #{boardTitle}, boardContent = #{boardContent}
		where boardid = #{boardId}
	</update>
	
	<delete id="deleteBoard" parameterType="integer">
		delete from board
		where boardid = #{boardId}
	</delete>
</mapper>